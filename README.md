# FB Account Auto Creator (Encoded)

⚠️ This script is encoded and protected by MR-SABIT.

## How to Use in Termux:
```bash
git clone https://github.com/sabit670/fb-encoded.git
cd fb-encoded
python encoded.py
